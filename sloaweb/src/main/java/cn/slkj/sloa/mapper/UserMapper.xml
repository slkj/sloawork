<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.slkj.sloa.mapper.UserMapper">

    <resultMap id="userList" type="cn.slkj.sloa.model.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
    </resultMap>


    <select id="selectUser" parameterType="cn.slkj.sloa.model.User" resultType="cn.slkj.sloa.model.User">
        SELECT * FROM user WHERE username=#{username} AND password=#{password}
    </select>
    <insert id="insertUser" parameterType="cn.slkj.sloa.model.User" flushCache="true">
        INSERT INTO user (id,username,password) VALUES (#{id},#{username},#{password})
    </insert>
    <update id="updateUser" parameterType="cn.slkj.sloa.model.User">
        UPDATE user SET password=#{password} WHERE id=#{id}
    </update>
    <delete id="deleteUser" parameterType="int">
        DELETE FROM user WHERE id=#{userId}
    </delete>

    <select id="selectUserLsit"  resultMap="userList">
        SELECT * FROM user
    </select>
</mapper>